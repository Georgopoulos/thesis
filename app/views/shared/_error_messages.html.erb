<% if obj.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-error">
			<%= "#{obj.errors.count} λάθη εμπόδισαν την υποβολη αυτής της φόρμας." %>
		</div>
		<ul>
			<% obj.errors.full_messages.each do |msg| %>
				<li>* <%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end%>